[build]
builder = "NIXPACKS"
buildCommand = "cd relay-server && npm install"

[deploy]
startCommand = "cd relay-server && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
